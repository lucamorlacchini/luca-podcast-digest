# Podcast Digest

Digest quotidiano via email dei nuovi episodi (non ascoltati) dei podcast seguiti su Spotify.
Repo privato personale — non collegato a nessun ambiente aziendale.

Vedi `config.example.json` per le variabili richieste in `config.json` (mai committare `config.json` in un repo pubblico).
